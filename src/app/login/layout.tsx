export const metadata = {
  title: 'PublimarTools',
  description: 'Sistema de gestión para Publimar - Iniciar sesión',
}

export default function LoginLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children;
}
